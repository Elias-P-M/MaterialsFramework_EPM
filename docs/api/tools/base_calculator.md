# BaseCalculator

The `BaseCalculator` abstract base class is the foundation for all ML calculator implementations. It provides `relax()` for geometry optimization and `calculate()` for single-point energy/force/stress evaluation.

See the full API documentation on the [Base Classes](../calculators/base.md#basecalculator) page.
